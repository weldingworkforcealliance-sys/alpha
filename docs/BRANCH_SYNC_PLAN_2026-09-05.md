# Branch reconciliation plan

The release branch is being reconciled onto the current `main` tree while preserving only the approved planner/attendance overlays. Unrelated newer `main` work remains authoritative.

Overlays retained:
- global Teaching Console and student display
- planner navigation and class selection adjustments
- Training Mode console reuse
- paired attendance UI/admin
- attendance migrations and report worker
- release validation documentation

No unrelated branch-era files should replace newer `main` content.